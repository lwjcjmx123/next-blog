{
  "functions": {
    "app/api/**/*.ts": {
      "maxDuration": 30
    }
  },
  "env": {
    "BLOB_READ_WRITE_TOKEN": "@blob-read-write-token",
    "DATABASE_URL": "@database-url",
    "JWT_SECRET": "@jwt-secret"
  },
  "build": {
    "env": {
      "BLOB_READ_WRITE_TOKEN": "@blob-read-write-token",
      "DATABASE_URL": "@database-url",
      "JWT_SECRET": "@jwt-secret"
    }
  }
}